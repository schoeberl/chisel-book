  object State extends ChiselEnum {
    val fetch, execute = Value
  }
  import State._

  val stateReg = RegInit(fetch)

  switch(stateReg) {
    is(fetch) {
      stateReg := execute
    }
    is(execute) {
      stateReg := fetch
    }
  }
